<!DOCTYPE html>
<html>
  <head>
    <title>Myapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <header>
      <h2>Ruby on Rails スケジュール管理アプリ</h2>
    </header>
    <div class = alert>
      <% flash.each do |message_type, message| %>
        <%= message %>
      <% end %>
    </div>
    <%=yield%>
    <footer>
      <h2>現在の日時：<%=Date.today.strftime("%Y年%m月%d日")%>  スケジュール合計：<%= Post.all.count %></h2> 
    </footer>
  </body>
</html>
